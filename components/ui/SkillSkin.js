const { default: Image } = require("next/image")

const SkillsSkin = () => {
    const code = ['react', 'next', '']
    return (
        <div className={styles.parent}>
            
        </div>
    )
}